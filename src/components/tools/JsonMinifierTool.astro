<div class="space-y-3" data-tool="json-minify">
  <div class="space-y-2">
    <label class="text-xs uppercase tracking-wide text-muted" for="json-minify-input">JSON</label>
    <textarea
      id="json-minify-input"
      class="min-h-[140px] w-full rounded-lg border border-border bg-bg px-3 py-2 text-sm text-text"
      placeholder='{"key":"value"}'
    ></textarea>
  </div>
  <button
    class="rounded-lg border border-border px-3 py-2 text-sm font-medium text-text hover:border-primary"
    type="button"
    data-run
  >
    Minify
  </button>
  <div class="space-y-2">
    <label class="text-xs uppercase tracking-wide text-muted" for="json-minify-output">Minified</label>
    <textarea
      id="json-minify-output"
      class="min-h-[140px] w-full rounded-lg border border-border bg-bg px-3 py-2 text-sm text-text"
      readonly
    ></textarea>
  </div>
  <p class="text-xs text-muted" data-error></p>
  <script is:inline>
    (() => {
      const root = document.querySelector('[data-tool="json-minify"]');
    if (root) {
      const input = root.querySelector("#json-minify-input");
      const output = root.querySelector("#json-minify-output");
      const run = root.querySelector("[data-run]");
      const error = root.querySelector("[data-error]");
      run?.addEventListener("click", () => {
        if (!input || !output || !error) return;
        try {
          const value = JSON.parse(input.value);
          output.value = JSON.stringify(value);
          error.textContent = "";
        } catch (err) {
          output.value = "";
          error.textContent = "Invalid JSON.";
        }
      });
    }
    })();
  </script>
</div>






